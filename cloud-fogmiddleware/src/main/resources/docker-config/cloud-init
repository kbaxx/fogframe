#cloud-config
coreos:
  update:
    reboot-strategy: off
  etcd:
    discovery: https://discovery.etcd.io/61180ef51686b07dd26dac24978c824d
    addr: $private_ipv4:4001
    peer-addr: $private_ipv4:7001
  units:
    - name: etcd.service
      command: start
    - name: docker-tcp.socket
      command: start
      enable: true
      content: |
        [Unit]
        Description=Docker Socket for the API

        [Socket]
        ListenStream=2375
        BindIPv6Only=both
        Service=docker.service

        [Install]
        WantedBy=sockets.target

